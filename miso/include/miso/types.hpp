#pragma once

namespace miso {

using Real = float;

enum class Sign { Pos, Neg };

enum class Direction { X, Y, Z };

enum class Face { Pos, Neg };

enum class Side { INNER, OUTER };

}  // namespace miso
