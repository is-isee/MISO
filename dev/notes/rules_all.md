# コーディング全般のルール

- パスの解決はリロケータブルかつカレントディレクトリ非依存な設計を推奨する
  - リロケータブル: プロジェクト一式をファイルシステムのどの場所に移動しても動く
    - 例: 絶対パスをハードコードしない
  - カレントディレクトリ非依存: どこから実行しても動く
    - 例: `config.yaml` からの相対パスで `data` ディレクトリを作成
    - 例: `app_clean.sh` からの相対パスで `data` ディレクトリを削除
